export const greeting = 'Hello from the agent-eslint-config demo'

/**
 * Emphasise a message by upper-casing it.
 *
 * @param message - the text to emphasise
 * @returns the message in upper case
 */
export function emphasise(message: string): string {
  return message.toUpperCase()
}
