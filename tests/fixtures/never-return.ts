// Physical on-disk fixture matched by tests/fixtures/tsconfig.json's include glob.
// Its presence lets the type-aware RuleTester resolve parser services against a
// real project path (not just the inferred default project); the RuleTester
// overrides this content per test case with the code under test.
export {}
